let day = "Saturday";
let alarm = "";


let alarmMessages = {
    weekendAlarm: "No alarm needed",
    weekDayAlarm: "Get up",
};

if (day == "Saturday || day == "Sunday) {
  alarm = alarmMessages.weekendAlarm

} else {

    al
}